apiVersion: "networking.k8s.io/v1"
kind: "Ingress"
metadata:
  name: "web-ingress-melih"
  namespace: "worldweb-melih"
spec:
  defaultBackend:
    service:
      name: "web-loadbalancer-melih"
      port:
        number: 8080
