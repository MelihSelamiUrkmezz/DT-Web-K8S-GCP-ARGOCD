apiVersion: "networking.k8s.io/v1"
kind: "Ingress"
metadata:
  name: "web-melih"
  namespace: "worldweb-melih"
spec:
  defaultBackend:
    service:
      name: "web-melih"
      port:
        number: 8080
